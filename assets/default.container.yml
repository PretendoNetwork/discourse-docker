# Basics for web_only
templates:
  - "templates/web.template.yml"
  - "templates/web.ratelimited.template.yml"

# Plugins
hooks:
  after_code:
    - exec:
        cd: $home/plugins
        cmd: [] # Add git clones here for plugins
